name: ci
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: make sync-resources
      - run: go vet ./...
      - run: go test ./...
      - run: go build -o dmux-verify ./cmd/dmux-verify

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: make sync-resources
      - run: |
          mkdir -p dist
          GOOS=darwin GOARCH=arm64 go build -o dist/dmux-verify-darwin-arm64 ./cmd/dmux-verify
          GOOS=darwin GOARCH=amd64 go build -o dist/dmux-verify-darwin-amd64 ./cmd/dmux-verify
          GOOS=linux GOARCH=amd64 go build -o dist/dmux-verify-linux-amd64 ./cmd/dmux-verify
          GOOS=linux GOARCH=arm64 go build -o dist/dmux-verify-linux-arm64 ./cmd/dmux-verify
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
